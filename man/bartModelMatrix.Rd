% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bartModelMatrix.R
\name{bartModelMatrix}
\alias{bartModelMatrix}
\title{Create a BART Model Matrix}
\usage{
bartModelMatrix(
  X,
  numcut = 0L,
  usequants = FALSE,
  type = 7,
  rm.const = FALSE,
  cont = FALSE,
  xinfo = NULL
)
}
\arguments{
\item{X}{A factor, vector, matrix, data frame, or \code{NULL} containing
predictors.}

\item{numcut}{Integer number of cut points to compute for each predictor.
If \code{0}, only the numeric model matrix is returned.}

\item{usequants}{Logical; if \code{TRUE}, use empirical quantiles for cut points.}

\item{type}{Quantile algorithm passed to \code{\link[stats:quantile]{stats::quantile()}}.}

\item{rm.const}{Logical; if \code{TRUE}, remove constant predictor columns.}

\item{cont}{Logical; if \code{TRUE}, use equally spaced cut points over the
observed range.}

\item{xinfo}{Optional list or matrix of user-supplied cut points.}
}
\value{
If \code{numcut = 0}, a numeric matrix. Otherwise, a list containing the
model matrix \code{X}, cut point counts \code{numcut}, retained columns \code{rm.const},
cut points \code{xinfo}, and original variable groups \code{grp}.
}
\description{
Converts vectors, factors, matrices, or data frames into the numeric model
matrix representation used by the BART sampler. When requested, the function
also computes predictor cut points and tracks removed constant columns.
}
\examples{
predictors <- data.frame(
  x1 = c(0.1, 0.4, 0.8, 1.2),
  group = factor(c("a", "b", "a", "b"))
)

mm <- bartModelMatrix(predictors, numcut = 3, rm.const = TRUE)
str(mm)

bartModelMatrix(predictors)

}
